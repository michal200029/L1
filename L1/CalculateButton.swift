import SwiftUI

struct CalculateButton: View {
    var body: some View {
        Button(action: {}) {
                        Text("OBLICZ")
                            .frame(width: 315, height: 60)
                            .border(Color.black, width: 2).font(.largeTitle)
                    }
    }
}
